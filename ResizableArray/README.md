Resizable Array

A basic C++ implementation of a dynamic, resizable array.

Features:
1) Dynamic resizing: Automatically doubles its capacity when elements are added beyond current capacity.
2) Custom constructors: A default, parameterized and copy constructor for flexible initialization.
3) Basic operations: Supports basic array operations such as push_back, pop_back, 